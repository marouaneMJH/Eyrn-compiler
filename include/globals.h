/**
 * A header File that contain the global variables used by the app
 * each compiler layer had it's own globals
 *
 */

#ifndef GLOBALS_H
#define GLOBALS_H

// Lexical analysis.

extern int line_n;

// Syntax analysis

// Semantic analysis
#endif